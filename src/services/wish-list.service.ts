import { Injectable } from '@angular/core';
import { Observable } from 'rxjs';
import { getFirestore, doc, getDoc, updateDoc, arrayUnion, arrayRemove, getDocs, collection } from 'firebase/firestore';  

@Injectable({
  providedIn: 'root'
})
export class WishlistService {
  // Instancia de Firestore
    // Declaración de la instancia de AngularFireDatabase

  constructor() {
  
  }

  // Obtener los productos desde Realtime Database


  // Obtener el wishlist del usuario desde Firestore
 

  // Agregar un producto al wishlist del usuario

}



